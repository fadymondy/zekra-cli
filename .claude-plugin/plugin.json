{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "zekra",
  "displayName": "Zekra \u2014 agent memory organ",
  "version": "0.1.2",
  "description": "Give Claude Code a persistent, hybrid-recall memory brain. Bundles the Zekra MCP (recall/retain + brains + datasources), memory-first slash commands, a curator agent, and an onboarding connector. Powered by the `zekra` CLI.",
  "author": {
    "name": "togo-framework",
    "url": "https://zekra.dev"
  },
  "homepage": "https://zekra.dev",
  "repository": "https://github.com/fadymondy/zekra-cli",
  "license": "MIT",
  "keywords": [
    "memory",
    "mcp",
    "zekra",
    "recall",
    "rag",
    "knowledge",
    "brain",
    "ai-native"
  ],
  "userConfig": {
    "api_url": {
      "type": "string",
      "title": "Zekra API URL",
      "description": "Base URL of the Zekra app (default https://app.zekra.dev).",
      "default": "https://app.zekra.dev"
    },
    "api_token": {
      "type": "string",
      "title": "Zekra token (cbt_\u2026)",
      "description": "Your access token. Mint one with `zekra auth token new <name>` or ask a brain admin.",
      "sensitive": true,
      "required": true
    },
    "default_namespace": {
      "type": "string",
      "title": "Default brain (optional)",
      "description": "Bind this session to one brain so tools default to it (e.g. think-os). Leave blank to pass namespace per call."
    },
    "auto_recall": {
      "type": "boolean",
      "title": "Auto-recall on every prompt",
      "description": "When on, each prompt is used to auto-recall relevant memories from the default brain and inject them as context (pull). Needs a default brain + a saved token.",
      "default": false
    },
    "inject_rules": {
      "type": "boolean",
      "title": "Inject memory-first rules at session start",
      "description": "Inject the recall\u2192answer\u2192retain discipline into every session (default on).",
      "default": true
    }
  },
  "mcpServers": {
    "zekra": {
      "command": "zekra",
      "args": [
        "mcp"
      ],
      "env": {
        "ZEKRA_API_URL": "${user_config.api_url}",
        "ZEKRA_TOKEN": "${user_config.api_token}",
        "ZEKRA_DEFAULT_NAMESPACE": "${user_config.default_namespace}"
      }
    }
  },
  "hooks": "./hooks/hooks.json"
}
